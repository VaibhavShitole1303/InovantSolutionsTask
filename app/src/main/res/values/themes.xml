<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.InovantSolutionsTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.InovantSolutionsTask" parent="Base.Theme.InovantSolutionsTask" />

    <style name="ButtonStyleMedium">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dp_50</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">@dimen/sp_16</item>
        <!-- <item name="android:background">@drawable/rc_login_bg</item>-->
    </style>

    <dimen name="dp_50">50dp</dimen>
</resources>